.Side-bar
  .Side-bar__top
    .top-items
      .top-items__name
        = current_user.name
      %ul.top-icon__list
        %li.top-icon
          = link_to "#" do
            = icon('far','far fa-edit')
        %li.top-icon
          = link_to edit_user_path(current_user) do
            = icon('fas','fas fa-cog')
  .Side-bar__group
    .group-top
      .group-top__name
        techcamp
      .group-top__message
        まだメッセージがありません。