.Side-bar
  .Side-bar__top
    .top-items
      .top-items__name
        = current_user.name
      %ul.top-items-list
        %li.top-items__icon
          = link_to "#" do
            = icon('far','edit')
        %li.top-items__icon
          = link_to edit_user_path(current_user) do
            = icon('fas','cog')
  .Side-bar__group
    .group-top
      .group-top__name
        techcamp
      .group-top__message
        まだメッセージがありません。